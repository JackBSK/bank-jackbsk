// empezamos nuestro proyecto node 'npm init --yess'
// instalamos dotenv para proteger las variables de entorno y creamos el archivo con .dotenv
// creamos el archivo .gitignore, para que evitemos subir las cosas al repositorio
// instalamos sequelize y postgres con el comando "npm i sequelize pg pg-hstore"
// Instalamos express con npm i express
// instalmos nodemon con "npm i nodemon --save-dev"
// iniciamos nuestro proyecto de sequelize con el comando sequelize-cli init
// en la parte de config cambiamos la extension del archivo a .js e importamos dotenv ahi con "const dotenv = require('dotenv').config();"
// configuramos las variables de entorno en config.js y en models/index.js cambiamos la temrinacion del archivo igual a js(adentro viene que cambiamos)
// creamos un modelo con el comando "sequelize-cli model:generate --name account_types --attributes name:string,description:string" y con esto se creo la migracion
// hacemos la creaciond e una tabla con el comando "sequelize-cli db:migrate"
